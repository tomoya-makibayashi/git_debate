<!DOCTYPE html>
<html>
  <head>
    <title>RonPa</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="header_title">
      RonPa<span>~テーマに沿って意見を戦わせよう~</span>
      </div>
      <div class="header_menu">
          <%= link_to '一覧', root_path, class: "header_menu_btn" %>
          <%= link_to '新規', new_theme_path, class: "header_menu_btn" %>
          <% if user_signed_in? %>
          <%= link_to '戦績', user_path(current_user), class: "header_menu_btn" %>
          <%= link_to '撤退', destroy_user_session_path, method: :delete, class: "header_menu_btn" %>
          <% else %>
          <%= link_to '参戦', new_user_session_path, class: "header_menu_btn" %>
          <% end %>
        </ul>
      </div>
    </header>
    <main class="main">
    <%= yield %>
    </main>
    <footer class="footer">
      <div class="footer_text">©️Makibayashi.Tomoya 2021</div>
    </footer>
  </body>
</html>
